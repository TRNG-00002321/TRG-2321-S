# Week 7: API Testing & Selenium - Coding Assessment

This assessment covers **REST Assured**, **API testing**, **Selenium WebDriver (Java)**, **locator strategies**, and the **Page Object Model**. Complete all 5 exercises.

---

## Exercise 1: REST Assured - GET Request Validation (Beginner)

Write REST Assured tests to validate a public API endpoint that returns user data.

**API Endpoint:** `https://jsonplaceholder.typicode.com/users/1`

**Requirements:**
- Verify the response status code is 200
- Validate the response content type is JSON
- Assert that the response body contains specific fields: `id`, `name`, `email`
- Verify the `id` field equals 1
- Check that the `email` field contains "@"

**Starter Code:**
```java
import io.restassured.RestAssured;
import io.restassured.response.Response;
import org.junit.jupiter.api.Test;
import static io.restassured.RestAssured.*;
import static org.hamcrest.Matchers.*;

class UserApiGetTest {

    private static final String BASE_URL = "https://jsonplaceholder.typicode.com";
    
    @Test
    void testGetUser_statusCode200() {
        // TODO: Send GET request to /users/1 and verify status code 200
        given()
            .baseUri(BASE_URL)
        .when()
            // TODO: Complete the request
        .then()
            // TODO: Assert status code
    }
    
    @Test
    void testGetUser_contentTypeJson() {
        // TODO: Verify response content type is JSON
    }
    
    @Test
    void testGetUser_bodyContainsRequiredFields() {
        // TODO: Use hasKey() matcher to verify id, name, email exist
    }
    
    @Test
    void testGetUser_idEqualsOne() {
        // TODO: Use body() assertion to verify id equals 1
    }
    
    @Test
    void testGetUser_emailContainsAtSymbol() {
        // TODO: Verify email field contains "@"
    }
}
```

---

## Exercise 2: REST Assured - POST Request with Body (Beginner)

Write tests for creating a new resource via POST request.

**API Endpoint:** `https://jsonplaceholder.typicode.com/posts`

**Request Body (JSON):**
```json
{
    "title": "Test Post",
    "body": "This is a test post body",
    "userId": 1
}
```

**Requirements:**
- Send a POST request with JSON body
- Verify status code is 201 (Created)
- Assert that the response contains an `id` field (generated by server)
- Validate that the response echoes back the `title` and `body` values you sent

**Starter Code:**
```java
import io.restassured.http.ContentType;
import org.junit.jupiter.api.Test;
import static io.restassured.RestAssured.*;
import static org.hamcrest.Matchers.*;
import java.util.HashMap;
import java.util.Map;

class PostApiCreateTest {

    private static final String BASE_URL = "https://jsonplaceholder.typicode.com";
    
    @Test
    void testCreatePost_statusCode201() {
        Map<String, Object> requestBody = new HashMap<>();
        // TODO: Populate the request body map with title, body, userId
        
        given()
            .baseUri(BASE_URL)
            .contentType(ContentType.JSON)
            .body(requestBody)
        .when()
            // TODO: Send POST request to /posts
        .then()
            // TODO: Assert status code 201
    }
    
    @Test
    void testCreatePost_responseContainsId() {
        // TODO: Verify response has id field that is notNullValue()
    }
    
    @Test
    void testCreatePost_responseEchoesTitle() {
        // TODO: Verify response title equals "Test Post"
    }
}
```

---

## Exercise 3: Selenium WebDriver - Navigation and Element Interaction (Beginner)

Write Selenium tests to interact with a web form on a demo site.

**Target URL:** `https://the-internet.herokuapp.com/login`

**Requirements:**
- Navigate to the login page
- Find the username and password input fields
- Enter credentials: username = "tomsmith", password = "SuperSecretPassword!"
- Click the login button
- Verify successful login by checking for a success message or URL change

**Starter Code:**
```java
import org.junit.jupiter.api.*;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import static org.junit.jupiter.api.Assertions.*;

class LoginPageTest {

    private WebDriver driver;
    private static final String BASE_URL = "https://the-internet.herokuapp.com/login";
    
    @BeforeEach
    void setup() {
        // TODO: Initialize ChromeDriver
        // TODO: Navigate to BASE_URL
    }
    
    @AfterEach
    void tearDown() {
        // TODO: Close the browser
    }
    
    @Test
    void testLogin_success() {
        // TODO: Find username field by id "username" and enter "tomsmith"
        // TODO: Find password field by id "password" and enter "SuperSecretPassword!"
        // TODO: Find login button and click it
        // TODO: Assert that URL contains "/secure" or success message is displayed
    }
    
    @Test
    void testLogin_invalidCredentials_showsError() {
        // TODO: Enter invalid credentials
        // TODO: Click login
        // TODO: Verify error message is displayed
    }
}
```

---

## Exercise 4: Selenium Locator Strategies (Intermediate)

Practice different locator strategies to find elements on a web page.

**Target URL:** `https://the-internet.herokuapp.com/challenging_dom`

**Requirements:**
Write methods to locate elements using ALL of the following strategies:
- By ID
- By Class Name
- By CSS Selector
- By XPath (both absolute and relative)
- By Link Text

**Starter Code:**
```java
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.junit.jupiter.api.*;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;

class LocatorStrategiesTest {

    private WebDriver driver;
    
    @BeforeEach
    void setup() {
        driver = new ChromeDriver();
        driver.get("https://the-internet.herokuapp.com/challenging_dom");
    }
    
    @AfterEach
    void tearDown() {
        driver.quit();
    }
    
    @Test
    void testFindById() {
        // The page has an element with a dynamic ID
        // TODO: Find the canvas element using a partial ID match with CSS selector
        // Hint: Use CSS selector like "[id^='canvas']" for IDs starting with 'canvas'
    }
    
    @Test
    void testFindByClassName() {
        // TODO: Find buttons with class "button"
        List<WebElement> buttons = driver.findElements(By.className("button"));
        assertTrue(buttons.size() >= 3, "Should find at least 3 buttons");
    }
    
    @Test
    void testFindByCssSelector() {
        // TODO: Find table cells in the first row using CSS selector
        // Hint: Use "table tbody tr:first-child td"
    }
    
    @Test
    void testFindByXPath_relative() {
        // TODO: Find the "edit" links in the table using relative XPath
        // Hint: //a[contains(text(),'edit')]
    }
    
    @Test
    void testFindByXPath_absolute() {
        // TODO: Find the first cell in the table using absolute XPath
        // Warning: Absolute XPath is brittle - this is for learning purposes
    }
    
    @Test
    void testFindByLinkText() {
        // TODO: Find links containing specific text
    }
}
```

---

## Exercise 5: Page Object Model Implementation (Intermediate)

Implement the Page Object Model pattern for a login flow.

**Target URL:** `https://the-internet.herokuapp.com/login`

**Requirements:**
1. Create a `LoginPage` class with:
   - WebElement fields for username, password, and login button
   - Methods: `enterUsername()`, `enterPassword()`, `clickLogin()`, `login(username, password)`
2. Create a `SecurePage` class with:
   - Method to verify successful login
   - Method to get the flash message text
3. Write tests that use these page objects

**Starter Code:**

**LoginPage.java:**
```java
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class LoginPage {
    
    private WebDriver driver;
    
    // Locators
    private By usernameField = By.id("username");
    private By passwordField = By.id("password");
    private By loginButton = By.cssSelector("button[type='submit']");
    
    public LoginPage(WebDriver driver) {
        this.driver = driver;
    }
    
    public void enterUsername(String username) {
        // TODO: Find element and send keys
    }
    
    public void enterPassword(String password) {
        // TODO: Find element and send keys
    }
    
    public void clickLogin() {
        // TODO: Find and click login button
    }
    
    // Convenience method that combines all steps
    public SecurePage login(String username, String password) {
        // TODO: Call enterUsername, enterPassword, clickLogin
        // TODO: Return new SecurePage instance
        return null;
    }
}
```

**SecurePage.java:**
```java
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class SecurePage {
    
    private WebDriver driver;
    
    private By flashMessage = By.id("flash");
    private By logoutButton = By.cssSelector("a[href='/logout']");
    
    public SecurePage(WebDriver driver) {
        this.driver = driver;
    }
    
    public boolean isLoginSuccessful() {
        // TODO: Check if flash message contains "You logged into a secure area"
        return false;
    }
    
    public String getFlashMessage() {
        // TODO: Return the text of the flash message
        return null;
    }
    
    public void clickLogout() {
        // TODO: Click the logout button
    }
}
```

**LoginTest.java (using Page Objects):**
```java
import org.junit.jupiter.api.*;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import static org.junit.jupiter.api.Assertions.*;

class LoginTestWithPOM {
    
    private WebDriver driver;
    private LoginPage loginPage;
    
    @BeforeEach
    void setup() {
        driver = new ChromeDriver();
        driver.get("https://the-internet.herokuapp.com/login");
        loginPage = new LoginPage(driver);
    }
    
    @AfterEach
    void tearDown() {
        driver.quit();
    }
    
    @Test
    void testSuccessfulLogin() {
        // TODO: Use loginPage.login() to log in with valid credentials
        // TODO: Assert that SecurePage.isLoginSuccessful() returns true
    }
    
    @Test
    void testInvalidLogin() {
        // TODO: Use loginPage to attempt login with invalid credentials
        // TODO: Verify appropriate error handling
    }
}
```

---

## Submission Guidelines

1. Complete all exercises with working code
2. Ensure Selenium tests use proper waits where needed
3. REST Assured tests should handle potential network issues gracefully
4. Page Object classes should be reusable and follow single responsibility principle
5. Include meaningful assertions in all tests
